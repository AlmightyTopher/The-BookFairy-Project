Refactor/cleanup request for BookFairy:

Goal: Identify and safely remove dead files, legacy menu flows, and unused code while enforcing the current spec and constitutional rules. Maintain all existing user-facing functionality.

What to do:

Inventory + Reachability

Build a map of commands, handlers, flows, and utilities actually referenced at runtime.

Detect unreachable files, unused exports, legacy slash/menus not reachable from the single /bookfairy entrypoint, and orphaned tests/docs.

Flag anything that violates the spec’s constitutional rules: single entrypoint, no DM delivery, relay-only via Prowlarr/Readarr/qBittorrent, 5-item pagination.

Propose diffs

Prepare a short list of candidate removals/refactors grouped by risk:
A) Safe deletes (truly dead)
B) Safe rewires (rename/move/update imports)
C) Risky (behavior-adjacent; needs extra tests)

For each candidate, include: file(s), why it’s safe (no refs / superseded), and what tests cover the surviving path.

Apply minimal changes

Implement the A) Safe deletes and B) Safe rewires first. Keep changes surgical: no redesigns, no new features.

Update imports, exports, and docs to remove dangling references. Do not change public behavior, command names, pagination size, or relay behavior.

Run checks

Run typecheck, lint, and the full test suite.

If anything fails, automatically revert just the offending hunk and move it from A/B → C with a note why.

Single cleanup PR

Create one feature branch (e.g., cleanup/legacy-purge-YYYYMMDD).

Use small, logical commits (inventory → deletes → rewires → docs).

PR title: “Cleanup: remove dead files, legacy flows, and unused code (no behavior change)”.

PR body:

What was removed/rewired and why (with file lists).

Proof of safety: links to tests, coverage or call-graph notes.

Confirmation of constitutional compliance.

Test/lint/typecheck results summary.

Constraints (do not violate):

One Discord entrypoint with button routing only.

No DM file delivery of any kind.

Relay-only via Prowlarr/Readarr/qBittorrent.

5-item pagination everywhere.

Do not move or change how secrets are stored.

Preserve repo structure and existing commands (npm run dev, npm test).

Outputs I expect:

A brief “Inventory & Plan” section (the proposed A/B/C groups).

The actual diffs applied for A and B.

Test + lint + typecheck results.

A single ready-to-merge cleanup PR as described above.

Assume “yes” for obviously safe deletes/rewires; ask once if anything is ambiguous or looks risky.
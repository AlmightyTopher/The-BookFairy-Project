Create a **Menu & Interaction Tree** for the BookFairy Discord bot — **repository-agnostic** (assume you know nothing about file paths).

**Goal**
Discover and document exactly how the UI looks and behaves in Discord: the single slash entrypoint, all buttons/menus/states, and every transition. Produce a full tree with stable node IDs and explain how I can request precise changes later.

**Repository discovery (no assumptions)**

1. **Scan for Discord surface area**

   * Grep for likely markers: `SlashCommandBuilder`, `client.on('interactionCreate')`, `REST.put(Routes.applicationCommands`, `discord.js`, `@discordjs/builders`, component `customId`, `MessageActionRow/ButtonBuilder/StringSelectMenuBuilder`, “pagination”, “Page”, “Next”, “Prev”.
   * Enumerate: slash commands, component handlers (by `customId`), router modules, middleware, and any constants controlling pagination.
2. **Find specs/plans if they exist**

   * Look for `specs/**`, `docs/**`, `README*`, `CONTRIBUTION*`. If found, prefer current spec over code when they disagree, but list the mismatch.
3. **Trace reachability**

   * Build a call/route map from the entrypoint(s) → handlers → services.
   * Identify unreachable/legacy menus or flows (present in repo but not reachable). Mark them as “Legacy/Unlinked”.

**Constraints (must enforce regardless of what code currently does)**

* Single slash entrypoint (`/bookfairy`) with button routing only.
* No DM file delivery.
* Relay-only via Prowlarr/Readarr/qBittorrent.
* 5-item pagination everywhere.
* Don’t move/change secrets.
* Preserve current user-visible behavior.

**Deliverable (write the doc even if you had to infer)**
Write **`specs/ui-map.md`** (create directory if missing) containing:

1. **Overview** (2 short paragraphs): What appears after `/bookfairy`, how choices branch, and the constitutional rules you enforced.
2. **ASCII Interaction Tree** from `/bookfairy` downward. Include nodes for:

   * **Audiobooks** → Genre → Timeframe → Results (paginated) → Confirm → Relay
   * **By Title** → Results (paginated) → Confirm → Relay
   * **By Author** → Results (paginated/grouped) → Confirm → Relay
   * **Status** (latest 5)
   * **Help**
3. **Node Table** (markdown) with columns:

   * `ID` (stable, e.g., `BF-UI-ROOT`, `BF-UI-AUDIOBOOKS-GENRE`, `BF-UI-RESULTS-PAGE`)
   * `User Label` (exact Discord text)
   * `Component` (slash | button | select | embed)
   * `Paginated?` (Yes/No; if Yes, fixed 5 per page)
   * `Next States` (IDs)
   * `Back/Cancel` behavior
   * `Discovered Handler` (best guess path or “TBD”)
4. **Pagination Rules**: fixed 5 per page, how Next/Prev work, “Page X of Y” copy, and disabled-button behavior on edges.
5. **Validation & Errors**: empty results, relay failure, rate limits, missing env (test mode).
6. **Telemetry Hooks**: where correlation IDs are produced/logged in the flow.
7. **Change Request Cookbook** (so I can ask for precise edits):

   * Template: “Update `<ID>`: change **Label** to ‘X’, **Order** under parent `<PARENT-ID>` to N, **Target Handler** to `path/to/file.ts`. Also update affected `Next States` to \[…].”
   * Examples: rename a button; insert a new timeframe under Audiobooks; (if asked to change page size ≠ 5, respond “blocked by constitution”).
8. **Open Questions**: list any ambiguous labels/paths and propose defaults.

**Quality bar**

* Be explicit about exact labels users see.
* If multiple candidates exist (e.g., two “results” handlers), pick the one actually wired from the entrypoint; mark others “Legacy/Unlinked”.
* No code changes in this pass — documentation only.

**After writing**

* Add a short **Diff Plan** section: list mismatches between doc vs. code/spec (labels, missing handlers, extra legacy menus). For each mismatch, propose a minimal follow-up: rename, rewire, or delete (no refactors).

**If repo has no Discord code**

* Produce the same doc from the spec alone, clearly marking every handler path as “TBD”, and include a “Scaffold Needed” list.

**Output**

* The full markdown in `specs/ui-map.md`.
* A one-screen summary at the bottom: total nodes, paginated nodes, legacy/unlinked nodes, and any constitutional violations detected (with suggested fixes).

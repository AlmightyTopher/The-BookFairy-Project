// src/lib/relay-store.ts
// Stub for relay store (mocked in tests)

export function getRecentRelays(): Array<{
  id: string;
  at: number;
  state: string;
}> {
  // This is a stub - tests will mock this function
  // Real implementation would return recent relay operations
  return [];
}